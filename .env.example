# NOTE: docker-compose.yml's MariaDB service is no longer used — the backend
# now runs on Postgres via Supabase (see VERCEL.md). This file is kept only
# for anyone still running the legacy Docker Compose / VPS setup manually;
# update docker-compose.yml's db service to Postgres yourself if you do.
DB_ROOT_PASSWORD=change_me_root_password
DB_NAME=atg_apply1
DB_USER=atg_app
DB_PASSWORD=change_me_db_password
